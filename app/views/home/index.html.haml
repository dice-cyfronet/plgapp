= image_tag("logo-big.png", :border=>0, id: 'logo-big')

%h1.text-center
  = t('home.title')
%div
  = t('home.desc')

%h1.text-center
  = t('home.apps-title')

.row.apps-preview
  - App.all.each do |app|
    .col-md-4.center-block
      %i.text-center.mdi-action-perm-data-setting.appl-icon
      %h2.text-center
        = link_to app.name, root_url(subdomain: app.full_subdomain)


