.row
  .col-md-4
    = render('apps_menu')
  .col-md-8
    .well
      %h4
        = t('apps.edit')
        .pull-right
          .btn-group
            %button#save-app.btn.btn-xs.btn-create{ title: t('apps.save') }
              = icon('save', t('save'))
            :javascript
              $(function() {
                $('#save-app').on('click', function() {
                  $('#app_form').submit();
                });
              });
            = link_to t('cancel'), @app,
                      class: 'btn btn-xs btn-default',
                      title: t('cancel')
      %hr

      = render('form', app: @app)